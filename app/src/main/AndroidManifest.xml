<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.navoiynoma">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NavoiyNoma">
        <activity android:name=".Lison_matn" />
        <activity android:name=".gazal" />
        <activity android:name=".Xamsa" />
        <activity android:name=".Lison" />
        <activity android:name=".Gazallari" />
        <activity android:name=".Sherlari" />
        <activity android:name=".Hayoti" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Xamsa_matn" />
    </application>

</manifest>